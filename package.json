{
  "name": "shopware-cli-build-project-action",
  "version": "1.0.0",
  "description": "GitHub Action to build Shopware projects using shopware-cli",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "github-action",
    "shopware",
    "shopware-cli"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
